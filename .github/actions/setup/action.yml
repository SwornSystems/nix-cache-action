name: Setup
description: Common setup steps

runs:
  using: composite
  steps:
    - name: Host info
      shell: bash
      run: |
        uname --all
        cat /proc/cpuinfo

    - name: Install Nix
      uses: nixbuild/nix-quick-install-action@2c9db80fb984ceb1bcaa77cdda3fdf8cfba92035 # v34

    - name: Cache Nix
      uses: ./
      with:
        key: nix-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('**/*.nix', '**/flake.lock') }}
        restore-keys: |
          nix-${{ runner.os }}-${{ runner.arch }}-

    - name: Build shells
      shell: bash
      run: |
        for shell in $(nix flake show --json | jq -r '.devShells | to_entries[0].value | keys[] | select(startswith("ci"))'); do
          nix develop ".#${shell}" --command true
        done
